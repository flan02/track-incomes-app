import TransactionList from '@/components/(transactions)/TransactionList'
import React from 'react'

type Props = {}

const Listapage = (props: Props) => {
  return (
    <TransactionList />
  )
}

export default Listapage